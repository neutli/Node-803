#include "uicomponents.h"

// Implementation is mostly inline in header, 
// but having a cpp file ensures moc handles the Q_OBJECT macro correctly
// when added to CMake sources.

// Empty implementation file is sufficient if all methods are inline,
// but usually destructors or non-inline methods go here.
